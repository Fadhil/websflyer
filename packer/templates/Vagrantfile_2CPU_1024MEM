Vagrant.configure("2") do |config|
  config.vm.provider "virtualbox" do |v|
    v.memory = 1024
    v.cpus = 2
  end

  config.vm.box = "websflyer-release-image"

  # forwarded ports
  config.vm.network "forwarded_port", guest: 3000, host: 3000
end
